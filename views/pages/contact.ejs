<div id="contact" v-cloak>
  <div class="container">
    <h1 class="text-center">Formulario de contacto</h1>
    <div class="contact-form" v-if="!cloudSuccess">
<p class="text-center">¿Tiene alguna pregunta para nosotros? Tal vez algunos comentarios? Nos encanta hablar con los
  usuarios sobre HiperSoft y nos complace responder a nuestras preguntas sobre precios, planes de trabajo o soluciones
  empresariales. Envíenos una nota y nos pondremos en contacto con usted lo antes posible.</p>
      <hr/>
      <ajax-form action="deliverContactFormMessage" :syncing.sync="syncing" :cloud-error.sync="cloudError" @submitted="submittedForm()" :handle-parsing="handleParsingForm">
        <div class="form-group">
          <label for="full-name">Nombre</label>
          <input class="form-control" id="full-name" name="full-name" type="text"  :class="[formErrors.fullName ? 'is-invalid' : '']" v-model.trim="formData.fullName" placeholder="J. Emmanuel López Loera" autocomplete="name" focus-first>
<div class="invalid-feedback" v-if="formErrors.fullName">Por favor déjanos saber como llamarte.</div>
        </div>
        <div class="form-group">
          <label for="email-address">Correo electrónico</label>
          <input class="form-control" id="email-address" name="email-address" type="email"  :class="[formErrors.emailAddress ? 'is-invalid' : '']" v-model.trim="formData.emailAddress" placeholder="emmanuel@hipersoft.com" autocomplete="email">
          <div class="invalid-feedback" v-if="formErrors.emailAddress">Poner dirección de correo electronico valida.</div>
        </div>
        <div class="form-group">
          <label for="topic">Asunto</label>
          <input class="form-control" id="topic" name="topic" type="text"  :class="[formErrors.topic ? 'is-invalid' : '']" v-model.trim="formData.topic" placeholder="Mi problema es el siguiente:" autocomplete="none">
<div class="invalid-feedback" v-if="formErrors.topic">Por favor, elija un tema para su mensaje.</div>
        </div>
        <div class="form-group">
          <label for="message">Mensaje</label>
          <textarea class="form-control" id="message" name="message" :class="[formErrors.message ? 'is-invalid' : '']" v-model.trim="formData.message" placeholder="¿Por que no me llegó el correo electronico de registro?" autocomplete="none"></textarea>
          <div class="invalid-feedback" v-if="formErrors.message">El mensaje no puede estar vacio.</div>
        </div>
<p class="text-danger" v-if="cloudError"><small>Ocurrió un error al procesar su solicitud. Verifique su información y
    vuelva a intentarlo, o <a href="/contact"> póngase en contacto con el servicio de asistencia </a> si el error
    persiste.</small></p>
        <div class="form-group">
          <ajax-button type="submit" :syncing="syncing" class="btn btn-dark btn-lg btn-block">Enviar mensaje</ajax-button>
        </div>
      </ajax-form>
    </div>
    <div class="success-message" v-if="cloudSuccess">
<p class="text-center"> ¡Gracias por llegar! </p>
      
<hr />
       <p> Hemos recibido su mensaje, y alguien de nuestro equipo se comunicará con usted pronto. </p>
    </div>
  </div>
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>
